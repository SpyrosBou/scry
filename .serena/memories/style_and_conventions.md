Code uses CommonJS modules, 2-space indentation, semicolons, and single quotes; keep files ASCII unless they already contain non-ASCII. Prefer helper utilities in `utils/` over bespoke logic and only add inline comments for complex flows. Follow Conventional Commits 1.0.0 (feat/fix/chore/etc., <=72 char subject, breaking changes marked). README must always include a note pointing to the last update commit/version. Documentation (README, AGENTS, SPEC, docs/) must be updated alongside functional changes. Focus on harness accuracy/performance rather than whether target sites pass; keep test pages and site configs current.