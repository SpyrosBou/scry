Before finishing a task: (1) run `npm run lint` (and `npm run lint:fix` if needed) plus any impacted Playwright suites via `node run-tests.js --site <name> [suite flags]` or the shortcut scripts, and `npm run test:unit` when Node helpers change; cite report paths or artifacts in PR notes. (2) Update documentation (README version line, AGENTS, SPEC, docs/) so it reflects the change set. (3) Stage only your modifications, keep any tests/reports referenced, and do not commit unless explicitly requested.